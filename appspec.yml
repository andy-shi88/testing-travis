version: 0.0

os: linux

files:
  - source: ./
    destination: /home/ubuntu/travis/testing-travis
    overwrite: true

hooks:
  BeforeInstall:
    - location: ./backup.sh
  ApplicationStart:
    - location: ./restart.sh
